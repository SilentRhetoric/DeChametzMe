name: Validate DeChametzMe-frontend

on:
  workflow_call:

jobs:
  validate:
    runs-on: "ubuntu-latest"
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          version: 9
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          cache-dependency-path: DeChametzMe-frontend/package-lock.json
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Install poetry
        run: pipx install poetry

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "poetry"

      - name: Install algokit
        run: pipx install algokit

      - name: Install dependencies
        run: algokit project bootstrap all --project-name 'DeChametzMe-frontend'

      - name: Run linters
        run: algokit project run lint --project-name 'DeChametzMe-frontend'

      - name: Run unit tests
        run: algokit project run test --project-name 'DeChametzMe-frontend'

      - name: Build
        run: algokit project run build --project-name 'DeChametzMe-frontend'
